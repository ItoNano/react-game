const Hooks = (props) => {
    return (
        <div>
            <div>ステートの使い方</div>
            <div>Hooksとは</div>
            <div>StateはClassコンポーネント でしか使えなかったが、関数コンポーネントでも使えるようになった</div>
            <div>Hooks = クラスコンポーネントの機能に接続する、という意味</div>
        </div>
    );
};

export default Hooks;
