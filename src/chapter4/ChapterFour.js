const ChapterFour = (props) => {
    return (
        <div>
            これはチャプター4
            <div>{props.title}</div>
            <div>Reactの読み込みは実際は必要ない</div>
        </div>
    );
};

export default ChapterFour;
